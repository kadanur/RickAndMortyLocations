query GetLocations($page: Int) {
    locations(page: $page) {
        info {
            count, pages
        }
        results {
            ...LocationDetail
        }
    }
}

fragment LocationDetail on Location {
    id, name, type, dimension,
    residents {
        id, name, image
    }
}
